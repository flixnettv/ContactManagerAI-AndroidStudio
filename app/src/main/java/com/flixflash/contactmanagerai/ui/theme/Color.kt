package com.flixflash.contactmanagerai.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * FlixFlash Contact Manager AI
 * 
 * @module UI Colors
 * @description نظام الألوان المخصص لـ FlixFlash
 * @author FlixFlash Technologies
 * @version 1.0.0
 * 
 * مجموعة ألوان متطورة ومدروسة مع دعم:
 * - ألوان FlixFlash الأساسية
 * - ألوان الذكاء الاصطناعي
 * - ألوان حالات الطوارئ
 * - ألوان الوضع المظلم والفاتح
 * - إمكانية الوصول والتباين العالي
 */
object FlixFlashColors {
    
    // ═══════════════════════════════════════════════════════════════
    // الألوان الأساسية لـ FlixFlash
    // ═══════════════════════════════════════════════════════════════
    
    // الألوان الأساسية
    val Primary = Color(0xFF1E88E5)          // أزرق FlixFlash الأساسي
    val OnPrimary = Color(0xFFFFFFFF)        // أبيض على الأساسي
    val PrimaryContainer = Color(0xFFBBDEFB) // حاوي الأساسي
    val OnPrimaryContainer = Color(0xFF0D47A1) // نص على حاوي الأساسي
    
    // الألوان الثانوية
    val Secondary = Color(0xFF26A69A)        // أخضر مائي
    val OnSecondary = Color(0xFFFFFFFF)      // أبيض على الثانوي
    val SecondaryContainer = Color(0xFFB2DFDB) // حاوي الثانوي
    val OnSecondaryContainer = Color(0xFF004D40) // نص على حاوي الثانوي
    
    // الألوان الثلاثية
    val Tertiary = Color(0xFFFF7043)         // برتقالي دافئ
    val OnTertiary = Color(0xFFFFFFFF)       // أبيض على الثلاثي
    val TertiaryContainer = Color(0xFFFFCCBC) // حاوي الثلاثي
    val OnTertiaryContainer = Color(0xFFBF360C) // نص على حاوي الثلاثي
    
    // ألوان الخطأ
    val Error = Color(0xFFE53935)            // أحمر للأخطاء
    val OnError = Color(0xFFFFFFFF)          // أبيض على الخطأ
    val ErrorContainer = Color(0xFFFFCDD2)   // حاوي الخطأ
    val OnErrorContainer = Color(0xFFB71C1C) // نص على حاوي الخطأ
    
    // ═══════════════════════════════════════════════════════════════
    // الألوان الوضع الفاتح
    // ═══════════════════════════════════════════════════════════════
    
    val LightBackground = Color(0xFFFAFAFA)    // خلفية فاتحة
    val LightOnBackground = Color(0xFF1C1B1F)  // نص على خلفية فاتحة
    val LightSurface = Color(0xFFFFFFFF)       // سطح فاتح
    val LightOnSurface = Color(0xFF1C1B1F)     // نص على سطح فاتح
    val LightSurfaceVariant = Color(0xFFF5F5F5) // متغير السطح الفاتح
    val LightOnSurfaceVariant = Color(0xFF49454F) // نص على متغير السطح الفاتح
    val LightOutline = Color(0xFF79747E)       // خط خارجي فاتح
    val LightOutlineVariant = Color(0xFFCAC4D0) // متغير الخط الخارجي الفاتح
    
    // ═══════════════════════════════════════════════════════════════
    // الألوان الوضع المظلم
    // ═══════════════════════════════════════════════════════════════
    
    val DarkBackground = Color(0xFF121212)     // خلفية مظلمة
    val DarkOnBackground = Color(0xFFE6E1E5)   // نص على خلفية مظلمة
    val DarkSurface = Color(0xFF1E1E1E)        // سطح مظلم
    val DarkOnSurface = Color(0xFFE6E1E5)      // نص على سطح مظلم
    val DarkSurfaceVariant = Color(0xFF2D2D2D) // متغير السطح المظلم
    val DarkOnSurfaceVariant = Color(0xFFCAC4D0) // نص على متغير السطح المظلم
    val DarkOutline = Color(0xFF938F99)        // خط خارجي مظلم
    val DarkOutlineVariant = Color(0xFF49454F) // متغير الخط الخارجي المظلم
    
    // لون الحجاب
    val Scrim = Color(0xFF000000)              // أسود للحجاب
    
    // ═══════════════════════════════════════════════════════════════
    // ألوان الذكاء الاصطناعي المصري
    // ═══════════════════════════════════════════════════════════════
    
    val AIPrimary = Color(0xFF9C27B0)          // بنفسجي للذكاء الاصطناعي
    val AIOnPrimary = Color(0xFFFFFFFF)        // أبيض على AI
    val AIPrimaryContainer = Color(0xFFE1BEE7) // حاوي AI
    val AIOnPrimaryContainer = Color(0xFF4A148C) // نص على حاوي AI
    
    val AIAccent = Color(0xFF673AB7)           // بنفسجي داكن مميز
    val AIAccentContainer = Color(0xFFD1C4E9) // حاوي مميز AI
    val AIOnAccentContainer = Color(0xFF311B92) // نص على حاوي مميز AI
    
    val AIGradientStart = Color(0xFF9C27B0)    // بداية تدرج AI
    val AIGradientEnd = Color(0xFF673AB7)      // نهاية تدرج AI
    
    // ═══════════════════════════════════════════════════════════════
    // ألوان الحالات الوظيفية
    // ═══════════════════════════════════════════════════════════════
    
    val Success = Color(0xFF4CAF50)            // أخضر للنجاح
    val SuccessContainer = Color(0xFFC8E6C9)   // حاوي النجاح
    val OnSuccessContainer = Color(0xFF1B5E20) // نص على حاوي النجاح
    
    val Warning = Color(0xFFFF9800)            // برتقالي للتحذير
    val WarningContainer = Color(0xFFFFE0B2)   // حاوي التحذير
    val OnWarningContainer = Color(0xFFE65100) // نص على حاوي التحذير
    
    val Info = Color(0xFF2196F3)               // أزرق للمعلومات
    val InfoContainer = Color(0xFFBBDEFB)      // حاوي المعلومات
    val OnInfoContainer = Color(0xFF0D47A1)    // نص على حاوي المعلومات
    
    // ═══════════════════════════════════════════════════════════════
    // ألوان الطوارئ والإنذار
    // ═══════════════════════════════════════════════════════════════
    
    val Emergency = Color(0xFFD32F2F)          // أحمر طوارئ
    val EmergencyDark = Color(0xFFB71C1C)      // أحمر طوارئ داكن
    val EmergencyContainer = Color(0xFFFFCDD2) // حاوي الطوارئ
    val OnEmergencyContainer = Color(0xFF8F0000) // نص على حاوي الطوارئ
    
    val Alert = Color(0xFFF44336)              // أحمر تنبيه
    val AlertContainer = Color(0xFFFFEBEE)     // حاوي التنبيه
    
    // ═══════════════════════════════════════════════════════════════
    // ألوان أنواع المكالمات
    // ═══════════════════════════════════════════════════════════════
    
    val IncomingCall = Color(0xFF4CAF50)       // أخضر للمكالمات الواردة
    val OutgoingCall = Color(0xFF2196F3)       // أزرق للمكالمات الصادرة
    val MissedCall = Color(0xFFFF5722)         // أحمر برتقالي للمكالمات الفائتة
    val BlockedCall = Color(0xFF424242)        // رمادي للمكالمات المحجوبة
    val AICall = Color(0xFF9C27B0)             // بنفسجي للمكالمات بالذكاء الاصطناعي
    val RecordedCall = Color(0xFFFF9800)       // برتقالي للمكالمات المسجلة
    
    // ═══════════════════════════════════════════════════════════════
    // ألوان حالة الاتصال
    // ═══════════════════════════════════════════════════════════════
    
    val Online = Color(0xFF4CAF50)             // أخضر متصل
    val Offline = Color(0xFF9E9E9E)            // رمادي غير متصل
    val Busy = Color(0xFFFF9800)               // برتقالي مشغول
    val Away = Color(0xFFFFC107)               // أصفر بعيد
    val DoNotDisturb = Color(0xFFE53935)       // أحمر عدم الإزعاج
    
    // ═══════════════════════════════════════════════════════════════
    // ألوان كشف الإزعاج
    // ═══════════════════════════════════════════════════════════════
    
    val SpamDetected = Color(0xFFE53935)       // أحمر إزعاج مكتشف
    val SpamSuspicious = Color(0xFFFF9800)     // برتقالي مشبوه
    val SpamClean = Color(0xFF4CAF50)          // أخضر نظيف
    val SpamUnknown = Color(0xFF9E9E9E)        // رمادي غير معروف
    
    // ═══════════════════════════════════════════════════════════════
    // ألوان تقييم الثقة
    // ═══════════════════════════════════════════════════════════════
    
    val HighConfidence = Color(0xFF4CAF50)     // أخضر ثقة عالية
    val MediumConfidence = Color(0xFFFF9800)   // برتقالي ثقة متوسطة
    val LowConfidence = Color(0xFFE53935)      // أحمر ثقة منخفضة
    
    // ═══════════════════════════════════════════════════════════════
    // ألوان خاصة بالميزات
    // ═══════════════════════════════════════════════════════════════
    
    val VoiceRecording = Color(0xFFE91E63)     // وردي تسجيل صوتي
    val TextToSpeech = Color(0xFF3F51B5)       // أزرق نيلي تحويل النص لصوت
    val SpeechToText = Color(0xFF009688)       // أخضر مائي تحويل الصوت لنص
    val EgyptianDialect = Color(0xFFFF6F00)    // برتقالي ذهبي للهجة المصرية
    
    // ═══════════════════════════════════════════════════════════════
    // ألوان التدرجات
    // ═══════════════════════════════════════════════════════════════
    
    val GradientPrimaryStart = Color(0xFF1E88E5)
    val GradientPrimaryEnd = Color(0xFF1976D2)
    
    val GradientSecondaryStart = Color(0xFF26A69A)
    val GradientSecondaryEnd = Color(0xFF00695C)
    
    val GradientAIStart = Color(0xFF9C27B0)
    val GradientAIEnd = Color(0xFF673AB7)
    
    val GradientEmergencyStart = Color(0xFFE53935)
    val GradientEmergencyEnd = Color(0xFFD32F2F)
    
    // ═══════════════════════════════════════════════════════════════
    // ألوان الشفافية
    // ═══════════════════════════════════════════════════════════════
    
    val Transparent = Color(0x00000000)        // شفاف تماماً
    val SemiTransparent = Color(0x80000000)    // نصف شفاف
    val OverlayLight = Color(0x1A000000)       // طبقة فاتحة
    val OverlayMedium = Color(0x4D000000)      // طبقة متوسطة
    val OverlayDark = Color(0x80000000)        // طبقة داكنة
    
    // ═══════════════════════════════════════════════════════════════
    // ألوان إضافية للواجهة
    // ═══════════════════════════════════════════════════════════════
    
    val CardBackground = Color(0xFFFFFFFF)     // خلفية البطاقات
    val CardBackgroundDark = Color(0xFF2D2D2D) // خلفية البطاقات المظلمة
    val DividerColor = Color(0xFFE0E0E0)       // لون الفواصل
    val DividerColorDark = Color(0xFF424242)   // لون الفواصل المظلمة
    
    val RippleEffect = Color(0x1A1E88E5)       // تأثير الموجة
    val SelectionBackground = Color(0x331E88E5) // خلفية التحديد
    
    // ═══════════════════════════════════════════════════════════════
    // ألوان خاصة بالأيقونات
    // ═══════════════════════════════════════════════════════════════
    
    val IconPrimary = Color(0xFF1E88E5)        // أيقونة أساسية
    val IconSecondary = Color(0xFF757575)      // أيقونة ثانوية
    val IconDisabled = Color(0xFFBDBDBD)       // أيقونة معطلة
    val IconSuccess = Color(0xFF4CAF50)        // أيقونة نجاح
    val IconWarning = Color(0xFFFF9800)        // أيقونة تحذير
    val IconError = Color(0xFFE53935)          // أيقونة خطأ
    val IconAI = Color(0xFF9C27B0)             // أيقونة ذكاء اصطناعي
}

/**
 * ألوان خاصة بالمناطق المصرية
 */
object EgyptianRegionColors {
    val Cairo = Color(0xFFFFD700)       // ذهبي للقاهرة
    val Alexandria = Color(0xFF00BCD4)  // أزرق مائي للإسكندرية
    val UpperEgypt = Color(0xFF8BC34A)  // أخضر للصعيد
    val Delta = Color(0xFF4CAF50)       // أخضر للدلتا
    val Canal = Color(0xFF2196F3)       // أزرق للقناة
    val General = Color(0xFF9C27B0)     // بنفسجي عام
}

/**
 * مساعدات للألوان
 */
object ColorUtils {
    
    /**
     * الحصول على لون بشفافية مختلفة
     */
    fun Color.withAlpha(alpha: Float): Color {
        return this.copy(alpha = alpha)
    }
    
    /**
     * الحصول على لون أفتح
     */
    fun Color.lighter(factor: Float = 0.2f): Color {
        return Color(
            red = (red + (1f - red) * factor).coerceIn(0f, 1f),
            green = (green + (1f - green) * factor).coerceIn(0f, 1f),
            blue = (blue + (1f - blue) * factor).coerceIn(0f, 1f),
            alpha = alpha
        )
    }
    
    /**
     * الحصول على لون أغمق
     */
    fun Color.darker(factor: Float = 0.2f): Color {
        return Color(
            red = (red * (1f - factor)).coerceIn(0f, 1f),
            green = (green * (1f - factor)).coerceIn(0f, 1f),
            blue = (blue * (1f - factor)).coerceIn(0f, 1f),
            alpha = alpha
        )
    }
    
    /**
     * التحقق من كون اللون فاتح أم داكن
     */
    fun Color.isLight(): Boolean {
        val luminance = (0.299 * red + 0.587 * green + 0.114 * blue)
        return luminance > 0.5
    }
}